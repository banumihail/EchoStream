venv/
.venv/
__pycache__/
*.py[cod]
models/
*.mp4
*.wav
.vscode/
.idea/
.env

# Results and uploads
results/
uploads/

# FFmpeg binary (large files)
ffmpeg/

# Model cache (HuggingFace models - on D: instead of C:)
models_cache/

# Pip cache (redirected to D:)
.pip_cache/

# Ad-hoc test/scratch scripts at repo root (real unit tests in subdirs are tracked)
/test_*.py
install_ffmpeg.py

# Local-only / debug
.claude/
scratch/

# TLS certs / private keys (generate locally via tools/gen_cert.py)
certs/
# Visual companion (brainstorming) scratch
.superpowers/
.playwright-mcp/

# Thesis — lives in Overleaf, contains personal data (CNP/CI). NEVER push to the public repo.
docs/thesis-latex/
# Other local-only docs (separate Word project, demo scripts, report drafts)
docs/report/
docs/presentation/
EchoStream_SecurityProject_source.zip
